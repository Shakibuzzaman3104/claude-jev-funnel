---
name: Bug report
about: Something in jev.py or the skill does not work as documented
title: ""
labels: bug
assignees: ""
---

> Never paste an API key. Remove `OPENROUTER_API_KEY` / `TYPESAFE_API_KEY` values, tokens,
> and any private data from commands and output before you submit.

**What happened**

**What you expected**

**Command**

```bash
# The exact command, with secrets and private data removed
```

**Output (redacted)**

```text
```

**Environment**

- `python3 skills/jev/scripts/jev.py --version`:
- Provider (`openrouter` or `typesafe`):
- Model (if not the default):
- Python version (`python3 --version`):
- OS:
- Installed as a Claude Code plugin or copied manually:

**Does it reproduce with `--mock` or `--dry-run`?**
