## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `python3 -m unittest discover -s tests -v` passes
- [ ] The examples still run with `--dry-run` and `--mock`
- [ ] Docs updated (`README.md`, `docs/cli.md`, `skills/jev/SKILL.md`, `CHANGELOG.md`) where behavior changed
- [ ] No API keys, tokens, or private data in code, tests, examples, or output
